export type Vehicle = {
    id: string
    title: string
    brand: string
    price: number
    description: string
    year: number
    licensePlate: string
    color: string
    colorBox: string
    colorText: string
    favorite: boolean
}